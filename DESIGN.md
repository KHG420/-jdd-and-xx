# 桌边的你们 · 视觉约定

用户已确认人物与风格计划，以 14 张参考图为视觉权威，不重新选择视觉方向。

角色是界面主体。保留蜡笔黑发、暖棕轮廓与奶油皮肤。两人头身比一致，女孩红上衣深色短裤、男孩白上衣蓝紫短裤。采用内置 imagegen 生成的九宫格动作图与四格双人图。生成器未交付真实 alpha，图集使用绿幕，加载时做色键及边缘去溢色，缓存 RGBA 贴图。

桌面：Experience，透明、无窗口边框，脚边很淡的落地阴影。日常动作为轻呼吸与偶尔眨眼，交互动作短促有收尾；安静和系统减少动态效果优先。

控制：Operate，系统原生中文菜单栏、右键菜单；不用自绘设置面板。文本气泡奶油白底、深棕字，简短低频。气泡和角色外透明区域允许底层点击。

Windows 延续同一套人物、配色、动作与短句，通过系统托盘及右键菜单操作；用 Win32 layered window 实现透明窗口，以 GDI+ 缓存缩放后的精灵。macOS 使用 Core Animation 缓存贴图。平台扩展不重新设计人物或添加设置界面。Windows 按显示器 DPI 适配，macOS 使用原生 backing scale。

实现边界：原生二维精灵动画，姿态间短淡入淡出结合位移、轻微旋转、缩放，不伪称完整逐帧骨骼动画。
